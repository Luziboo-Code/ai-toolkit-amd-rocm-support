# AMD ROCm requirements for ai-toolkit.
#
# This mirrors requirements_base.txt with the packages that have no usable
# ROCm build removed, and adds the pins from requirements.txt. Install torch
# for your ROCm version first (see the AMD section of README.md), then:
#
#   pip install -r requirements-amd.txt
#
# Removed relative to requirements_base.txt:
#   bitsandbytes - no ROCm wheel; build it from source (see README.md) or skip
#   torchcodec   - CUDA-only video decode; audio/video datasets fall back to av
torchao==0.10.0
safetensors
git+https://github.com/huggingface/diffusers.git@c943837899b16cbae2f619b8dd4f7bb6f07dd81a
transformers==5.5.3
lycoris-lora==1.8.3
flatten_json
pyyaml
oyaml
tensorboard
kornia
invisible-watermark
einops
accelerate
toml
albumentations==1.4.15
albucore==0.0.16
pydantic
omegaconf
open_clip_torch
timm==1.0.22
prodigyopt
controlnet_aux==0.0.10
python-dotenv
hf_transfer
lpips
pytorch_fid
optimum-quanto==0.2.4
sentencepiece
huggingface_hub==1.23.0
peft==0.18.1
gradio
# inference engine http server (extensions_built_in/inference_engine)
fastapi>=0.115
uvicorn>=0.30
python-slugify
opencv-python
pytorch-wavelets==1.3.0
matplotlib==3.10.1
setuptools>=77.0.3
scipy==1.12.0
av==16.0.1
librosa==0.11.0
mutagen==1.47.0
